# 扶摇阁 — UI 高级感重设计方案

> 基于 PRD v15 章节，目标：Apple 级别精致感 × 金融专业信任感

---

## 一、设计理念

### 核心关键词

**克制 · 层次 · 流动 · 金色点缀**

- **克制**：减少颜色数量，信息层级分明，留白充足
- **层次**：白色卡片 + 阴影叠在暖灰背景上，深度感清晰
- **流动**：微动画（数字过渡、涟漪、fade+slide 页面切换）
- **金色点缀**：少量金色（CTA / 选中态 / 分割线）暗示财富属性，不喧宾夺主

### 对标参考

| 维度 | 参考 |
|------|------|
| 卡片质感 | Apple Wallet / 健康 App |
| 信息层级 | Robinhood / 富途牛牛 |
| 色彩控制 | Notion（少色、大量留白） |
| 字体选择 | 高端杂志排版（Vogue / Monocle） |

---

## 二、字体系统

| 角色 | 字体 | 字重 | 字号 | 行高 |
|------|------|------|------|------|
| Hero 数字（总资产） | Bodoni Moda | 700 | 36sp | 44sp |
| 大标题 | Bodoni Moda | 600 | 24sp | 32sp |
| 卡片标题 | Jost | 600 | 16sp | 24sp |
| 正文 | Jost | 400 | 14sp | 20sp |
| 辅助文本 / 标签 | Jost | 400 | 12sp | 16sp |
| 数字（金额、百分比） | Jost | 500-600 | 16-22sp | — |

> **为什么不用系统默认字体**：Bodoni Moda 的优雅衬线 + Jost 的几何现代感形成「经典 × 当代」对比，传递高级品牌感。这两个字体通过 Google Fonts 分发的 `downloadableFonts` 集成，无需打包字体文件。

### 字重使用规则

```
ExtraBold (700) → 仅 Hero 数字（总资产、今日收益主数字）
Bold (600)     → 分类卡片金额、页面标题
Medium (500)   → 次要数字、按钮文字
Regular (400)  → 正文、标签、辅助信息
```

---

## 三、色彩系统

### 3.1 浅色模式（默认）

```
页面背景   #F5F5F7  — 微暖浅灰（不是纯白，更有温度）
卡片表面   #FFFFFF  — 纯白浮在暖灰上，层次清晰
主文字     #1C1917  — 近黑（warm black，比 #000 更高级）
次要文字   #6B6560  — 暖灰棕，比常规 gray 更柔和
金色强调   #B7930A  — 选中的指示线 / CTA 按钮 / 分割装饰
金色浅底   #FEF9E7  — 极少数强调背景（如选中日期的底色）
上涨       #C0392B  — 稍暗的正红，不刺眼
上涨浅底   #FDF2F2  — 日历格子 / 标签背景
下跌       #27AE60  — 稍暗的翠绿，不荧光
下跌浅底   #EDF7F0  — 日历格子 / 标签背景
卡片阴影   #000000 · 3% ~ 8% opacity
分割线     #F0EFEC  — 比卡片边框更柔和
```

### 3.2 深色模式

```
页面背景   #0F172A  — 深海蓝黑
卡片表面   #1E293B  — slate 深灰
主文字     #F8FAFC  — 近白
次要文字   #94A3B8  — slate-400
金色强调   #F59E0B  — 深色下更亮
上涨       #EF4444  — 深色下需要更亮
下跌       #4ADE80  — 深色下需要更亮
```

### 3.3 资产分类色（仅用于图标和点缀）

| 资产类型 | 色值 | 用途 |
|----------|------|------|
| 存款 | `#4A6FA5`（稳重的蓝） | 图标、占比条 |
| 股票 | `#C0392B`（涨红色，保持一致） | 图标、占比条 |
| 基金 | `#8B5CF6`（紫色，trust） | 图标、占比条 |
| 黄金 | `#B7930A`（金色） | 图标、占比条 |

> **原则**：分类色只用在图标和细条上，不铺满整个卡片。卡片本身永远是白色。

---

## 四、间距系统

采用 4dp 基础网格：

| Token | 值 | 用途 |
|-------|-----|------|
| `xs` | 4dp | 图标与文字间距 |
| `sm` | 8dp | 同类信息内部间距 |
| `md` | 12dp | 卡片之间间距 |
| `lg` | 16dp | 卡片内边距 |
| `xl` | 20dp | 大卡片内边距、相关联卡片间距 |
| `2xl` | 24dp | Section 间距 |
| `3xl` | 32dp | 页面顶部/底部留白 |

---

## 五、圆角 & 阴影

| 组件 | 圆角 | 阴影 |
|------|------|------|
| 走势图/大卡片 | 20dp | `elevation = 3dp` |
| 资产分类卡片 | 16dp | `elevation = 2dp` |
| 列表项卡片（持仓） | 12dp | `elevation = 2dp` |
| 按钮 / FAB | 28dp (pill) | `elevation = 4dp` |
| Dialog / BottomSheet | 24dp (top) | `elevation = 8dp` |

---

## 六、全 App 连续滑动体系（v3 核心架构）

### 6.0 设计目标

**消灭一切 Tab 点击切换，改为连续滑动。** 用户从首页开始向右滑，可以无中断地穿越所有页面。

底部导航不再触发页面跳转，而是**快速定位到 Pager 中的锚点页面**。

### 6.1 页面序列（扁平化 HorizontalPager）

```
Page  0:  首页（净资产仪表盘）
Page  1:  投资 · 股票
Page  2:  投资 · 基金
Page  3:  投资 · 黄金
Page  4:  存款 · 持有中
Page  5:  存款 · 已到期
Page  6:  现金
Page  7:  信用
Page  8:  收益
Page  9:  设置
```

**总共 10 页，一个 `HorizontalPager`，userScrollEnabled = true。**

### 6.2 底部导航 → Pager 锚点映射

```
底部:  [首页]    [持仓]    [收益]    [设置]
        ↓         ↓         ↓         ↓
Page:   0         1         8         9
```

- 点「首页」→ `animateScrollToPage(0)`
- 点「持仓」→ `animateScrollToPage(1)`（投资·股票，持仓段第一页）
- 点「收益」→ `animateScrollToPage(8)`
- 点「设置」→ `animateScrollToPage(9)`
- **底部导航选中态跟随 `currentPage` 所在区间变化**

### 6.3 滑动规则

| 规则 | 说明 |
|------|------|
| 全程可滑 | `userScrollEnabled = true`，惯性滑动 |
| 无循环 | 第 0 页不能左滑，第 9 页不能右滑 |
| 二级优先 | 同一分类内的子页先滑完（股票→基金→黄金），再进入下一个分类（存款） |
| 底部同步 | 滑到 page 0 → 首页高亮；滑到 page 1-7 → 持仓高亮；page 8 → 收益；page 9 → 设置 |
| 首页资产网格 | 点击网格里的「现金」「存款」等，不再跳 page，而是 `animateScrollToPage(目标页)` |

### 6.3 Category 快速跳转导航条（v4 新增）

**问题**：10 页扁平 Pager 中，从股票(page 1) 到存款(page 4) 需滑 3 次。仅靠滑动无法快速跨越分类。

**方案**：在 Pager 上方（SectionIndicator 上方）新增一条可点击的分类导航条，**v17 增加分类汇总行**：

```
┌──────────────────────────────────────┐
│  [投资]    [存款]    [现金]   [信用] │  ← CategoryNavStrip
│   ●                                │     当前分类高亮+金色下划线
│                                      │
│  投资总计  ¥423,000  今日 +¥2,500   │  ← v17 新增：当前分类汇总
└──────────────────────────────────────┘
```

**规格**：
- 仅在 page 1-7 显示（投资段+存款段+现金+信用）
- 4 个分类 chip：投资 / 存款 / 现金 / 信用
- 当前分类 = 金色文字 + 2dp 底部指示线，其他 = 灰色文字
- 点击 → `animateScrollToPage(该分类第一页)`
- 导航条背景白色卡片，16dp 圆角，左右 margin 16dp
- 映射：投资→page 1, 存款→page 4, 现金→page 6, 信用→page 7

**v17 分类汇总行规格**：
- 位于 chip 行下方，一条细分割线隔开
- 左侧：分类名 + 总计（如「投资总计 ¥423,000」），14sp Jost 500
- 右侧：今日变动（涨跌色），14sp Jost 600，如「+¥2,500」
- 随当前 page 所在分类动态切换：

| 当前分类 | 汇总行内容 |
|----------|-----------|
| 投资 (page 1-3) | 投资总计 ¥423,000 / 今日 +¥2,500 |
| 存款 (page 4-5) | 存款总计 ¥512,350 / 今日利息 +¥45.20 |
| 现金 (page 6) | 现金余额 ¥45,000（今日不适用） |
| 信用 (page 7) | 应收净额 -¥30,000（今日不适用） |

### 6.4 顶部 Section 指示器（替代双层 Tab）

**v4 更新**：改为**可点击**的子项跳转。

既然没有 Tab 了，页面顶部需要一个轻量的**位置指示器**，告诉用户「你现在在哪、这个分类还有几页」。

#### 首页（Page 0）：无指示器

```
┌──────────────────────────────────────┐
│  净资产                               │
│  ¥ 586,320                           │
│  ...                                 │
└──────────────────────────────────────┘
```

#### 投资段（Page 1-3）：分类指示器 + 圆点 + 当前子类汇总（v17 增强）

```
┌──────────────────────────────────────┐
│                                      │
│   ┌────────────────────────────┐     │
│   │  投资                    │     │  ← 灰色文字 12sp
│   │                          │     │
│   │  股票  ·  基金  ·  黄金  │     │  ← 15sp, 当前页加粗+金色
│   │                          │     │
│   │  ●   ○   ○              │     │  ← 3 圆点，当前位置实心金
│   │                          │     │
│   │  股票市值  ¥358,000      │     │  ← 新增：当前子类总市值 + 今日
│   │  今日     +¥2,350 (+0.66%)│    │
│   └────────────────────────────┘     │
│                                      │
│  [股票组卡片列表 LazyColumn]          │
└──────────────────────────────────────┘
```

- 切换到 Page 2（基金）：汇总行变为「基金市值 ¥48,000 / 今日 +¥135 (+0.28%)」
- 切换到 Page 3（黄金）：汇总行变为「黄金市值 ¥17,000 / 今日 +¥15 (+0.09%)」
- 汇总数据随 Pager 位置实时变化

**指示器规格（v17 更新）：**
- 分类标签「投资」：12sp，`onSurfaceVariant`，Regular
- 子项名称（股票/基金/黄金）：15sp，当前选中 = SemiBold + `onSurface`，未选中 = Regular + `onSurfaceVariant`
- 圆点：8dp，金色实心（当前）/ 灰色空心（其他），间距 6dp
- 子项名称（股票/基金/黄金、持有中/已到期）**可点击**，点击 → `animateScrollToPage(目标页)`
- 子类汇总行：市值 14sp Jost 600 `onSurface`，今日涨跌 13sp Jost 500 涨跌色
- 位置跟随 Pager 滚动自然切换，也可手动点击跳转
- 背景白色卡片，16dp 圆角，轻微阴影，左右 margin 16dp

#### 存款段（Page 4-5）：同上结构 + 汇总

```
│   ┌────────────────────────────┐     │
│   │  存款                    │     │
│   │  持有中  ·  已到期       │     │
│   │  ●   ○                  │     │
│   │                          │     │
│   │  持有中估值  ¥512,350    │     │  ← 新增：当前子类总估值 + 今日
│   │  今日利息   +¥45.20      │     │
│   └────────────────────────────┘     │
```

- 切换到 Page 5（已到期）：汇总行变为「已到期本息 ¥150,401」

#### 现金 / 信用 / 收益 / 设置（Page 6-9）：简单标题

```
│  现金                               │  ← 18sp, SemiBold
│  [现金余额卡片 + 流水列表]            │
```

这些页面只有一个分类标签，没有子项圆点。

### 6.5 首页（Page 0）— v8 紧凑 2×2 网格

**v8 更新**：资产卡片改为 2×2 紧凑网格，居中展示，走势图移至页面最底部。

**设计目标**：用户打开首页关心三件事：总资产、今天收益、每类资产现在值多少。资产卡片用不同颜色区分，2×2 网格紧凑排列，视觉重心居中。

```
┌──────────────────────────────────────┐
│                                      │
│              总资产                   │  12sp, onSurfaceVariant
│           ¥ 586,320                  │  36sp, Serif Bold, 居中
│                                      │
│        今日  +1,250.30               │  22sp, 涨跌色, 居中
│              +0.21%                  │  16sp, 涨跌色
│                                      │
│        更新于 15:30                  │  10sp, 灰色
│                                      │
│  ┌────────────────────────────────┐  │
│  │  资产配置                      │  │  ← 16sp SemiBold
│  │  ████████████ ████ ██ █        │  │  ← 堆叠占比条
│  │  投资 71%     存款 21%  现金 5%│  │
│  └────────────────────────────────┘  │
│                                      │
│  ┌──────────────┐ ┌──────────────┐  │
│  │ 🟡 投资       │ │ 🔵 存款       │  │  ← 2×2 网格，等宽
│  │  ¥420,000    │ │  ¥120,000    │  │
│  │  +820 今日   │ │  +34 今日    │  │
│  └──────────────┘ └──────────────┘  │
│  ┌──────────────┐ ┌──────────────┐  │
│  │ 🟢 现金       │ │ 🟠 信用       │  │
│  │  ¥45,000     │ │  -¥30,000    │  │
│  │              │ │              │  │
│  └──────────────┘ └──────────────┘  │
│                                      │
│  ┌────────────────────────────────┐  │
│  │  资产走势          7天 30天 1年 │  │  ← 走势图放最下面
│  │  [折线图]                      │  │
│  │  净值 | 投资收益               │  │
│  └────────────────────────────────┘  │
│                                      │
└──────────────────────────────────────┘
```

### 信息层级（v8 调整后）

```
L0 — 总资产          36sp Serif 700    页面唯一焦点，居中
L1 — 今日总收益      22sp Bold         涨跌色，一眼知赚赔
L2 — 资产配置占比条   堆叠条+标签       快速了解资产结构
L3 — 2×2 资产网格    4 张紧凑卡片      每张 = 名称 + 总价值 + 今日收益
L4 — 资产走势图      折线图            7d/30d/1y 切换，页面收尾
L5 — 更新时间        10sp             最低调
```

### 2×2 资产网格卡片规格

每张卡片 = 顶部色条（4dp）+ 两行内容，半宽紧凑布局：

```
┌────────────────────┐
│ ════════════════   │  ← 顶部色条 4dp, 资产类型色
│  投资    ¥420,000   │  ← 行1: 名称(14sp Bold) + 总价值(16sp Bold)
│  +820 今日         │  ← 行2: 今日收益(13sp, 涨跌色)
└────────────────────┘
```

| 属性 | 值 |
|------|-----|
| 卡片 | `ElevatedCard`, 14dp 圆角, elevation=2dp |
| 卡片宽度 | `Modifier.weight(1f)`，两列等宽 |
| 顶部色条 | 4dp 高, 资产类型色, 卡片顶部圆角跟随 |
| 列间距 | 10dp |
| 行间距 | 10dp |
| 卡片内边距 | 10dp 水平, 8dp 垂直（色条下方） |
| 名称 | 14sp, SemiBold, onSurface |
| 总价值 | 16sp, Bold, onSurface（右对齐） |
| 今日收益 | 13sp, SemiBold, 涨跌色（无收益则不显示） |
| 最小高度 | 72dp（保证四张卡片等高） |

### 资产类型色 + 顶部色条映射

| 卡片 | 顶部色条色 | 总价值来源 | 今日收益来源 | 点击跳转 |
|------|----------|-----------|-------------|----------|
| 投资 | `gold` #B7930A | 股票+基金+黄金 持仓市值 | 三类持仓今日涨跌之和 | Page 1 (股票) |
| 存款 | `deposit` #4A6FA5 | active 存款的本金+累计利息 | 所有存款今日利息之和 | Page 4 (持有中) |
| 现金 | `cash` #2E7D32 | 现金余额 | — | Page 6 |
| 信用 | `receivable` #E67E22 | 应收 - 负债 | — | Page 7 |

### 网格排列顺序

```
Row 1:  投资（左上）    存款（右上）
Row 2:  现金（左下）    信用（右下）
```

按总价值绝对值降序：投资 > 存款 > 现金 > 信用（应收净额）。

### 交互

| 操作 | 行为 |
|------|------|
| **点卡片** | `animateScrollToPage(目标页)` |
| **点总资产数字** | 无操作（纯展示） |
| **下拉刷新** | 拉取实时价格 + 重算所有数据 |
| **长按卡片** | 弹出快捷菜单（投资→添加持仓，存款→添加存款，现金→入金/出金） |
| **下拉刷新** | 拉取实时价格 + 重算所有数据 |
| **右滑** | 自然进入投资·股票（连续 Pager） |

### 我的额外优化建议

| # | 优化点 | 说明 |
|---|--------|------|
| 1 | **数字动画** | 价格刷新后数字用 `animateFloatAsState` 过渡，500ms easeOut，不是瞬间跳变 |
| 2 | **骨架屏** | 启动时价格还没拉回来，卡片显示 shimmer 占位，避免空白闪烁 |
| 3 | **长按快捷操作** | 长按投资卡片 → 「添加持仓」；长按存款 → 「添加存款」；减少导航步数 |
| 4 | ~~总资产点按切换精度~~ | 已废弃：全部使用完整数字，不用"万"缩写 |
| 5 | **今日收益区域下拉展开** | 下拉总资产区域 → 展开显示近 7 天每日收益小列表（不需要跳到收益页） |
| 6 | **负债卡片视觉弱化** | 应收/负债卡片用更低的 elevation + 更淡的左边条，不跟资产卡片抢注意力 |
| 7 | **触觉反馈** | 卡片点击加 `hapticFeedback`，手感更扎实 |

---

## 七、各页面详细布局

### 7.1 投资 · 股票（Page 1）

```
┌──────────────────────────────────────────────┐
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │  投资                                │    │
│  │  股票  ·  基金  ·  黄金              │    │  ← 只读指示器
│  │  ●   ○   ○                          │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  ┌─ 贵州茅台 ──────────────────────────┐    │
│  │ 600519                    持有 100股 │    │
│  │ 成本 ¥1,680  现价 ¥1,720            │    │
│  │ ─────────────────────────────────   │    │
│  │ 持仓盈亏            今日            │    │
│  │ +¥4,000            +¥200           │    │
│  └─────────────────────────────────────┘    │
│                                              │
│  ┌─ 腾讯控股 ──────────────────────────┐    │
│  │ ...                                 │    │
│  └─────────────────────────────────────┘    │
│                                              │
│  [+] 添加股票 FAB                           │
└──────────────────────────────────────────────┘
```

### 7.2 投资 · 基金（Page 2）

- 布局与股票页相同，仅指示器圆点移到基金（○ ● ○）
- 数据源：`state.funds`
- FAB：「添加基金」

### 7.3 投资 · 黄金（Page 3）

- 布局与股票页相同，仅指示器圆点移到黄金（○ ○ ●）
- 数据源：`state.golds`
- FAB：「添加黄金」

### 7.4 存款 · 持有中（Page 4）

```
┌──────────────────────────────────────────────┐
│  ┌──────────────────────────────────────┐    │
│  │  存款                                │    │
│  │  持有中  ·  已到期                   │    │
│  │  ●   ○                              │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  ┌─ 工商银行定期 ───────────────────────┐    │
│  │ ¥201,832                             │    │
│  │ 今日利息 +¥15.07                     │    │
│  │ ─────────────────────────────────   │    │
│  │ 本金 ¥200,000 · 利率 2.75%          │    │
│  │ 已持 121 天 · 到期 2027/03/15       │    │
│  │ ████████████░░░░  65%              │    │
│  └─────────────────────────────────────┘    │
│                                              │
│  [+] 添加存款 FAB                           │
└──────────────────────────────────────────────┘
```

### 7.5 存款 · 已到期（Page 5）

- 指示器圆点 ○ ●
- 展示 status=matured/settled 的存款
- 卡片半透明灰色调，标注「已到期」
- 显示总利息、到期日、自动赎回提示

### 7.6 现金（Page 6）

```
┌──────────────────────────────────────────────┐
│  现金                                       │  ← 18sp SemiBold 标题
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │  现金余额                            │    │
│  │  ¥ 45,000.00                         │    │
│  │                                      │    │
│  │  [+ 入金]   [- 出金]                 │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  资金流水                                   │
│  ┌──────────────────────────────────────┐    │
│  │ 2026-05-20  手动存入     +10,000     │    │
│  │ 2026-05-15  手动取出      -2,000     │    │
│  │ ...                                 │    │
│  └──────────────────────────────────────┘    │
└──────────────────────────────────────────────┘
```

### 7.7 信用（Page 7）

```
┌──────────────────────────────────────────────┐
│  信用                                       │
│                                              │
│  ┌─ 应收款 ────────────────── [+] ──────┐    │
│  │  张三                     ¥20,000    │    │
│  │  2026-02-10 借出 · 预计 2026-08 归还 │    │
│  │  ───────────────────────────────    │    │
│  │  王五                     ¥10,000    │    │
│  │  合计应收  ¥30,000                   │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  ┌─ 负债 ──────────────────── [+] ──────┐    │
│  │  招行信用卡               ¥35,000    │    │
│  │  李四                     ¥25,000    │    │
│  │  合计负债  ¥60,000                   │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  应收净额  -¥30,000                          │
└──────────────────────────────────────────────┘
```

### 7.8 股票/基金/黄金卡片

```
┌──────────────────────────────────────┐
│ 贵州茅台 (600519)           [>]       │  名称 + 代码
│                                      │
│ ¥1,720.00                            │  当前价 22sp Bold
│ ▲ +20.00 (+1.18%) 今日               │  涨跌 16sp
│                                      │
│ ───────────────────────────────────  │  细分隔线
│                                      │
│ 持仓盈亏         今日盈亏             │
│ +¥4,000          +¥200               │  双列数据
│ +2.38%           +0.12%              │
│                                      │
│ 持有 100 股 · 成本 ¥1,680 · 市值 ¥172,000 │  12sp 次要信息
└──────────────────────────────────────┘
```

### 7.9 存款卡片

```
┌──────────────────────────────────────┐
│ 工商银行定期                 [>]      │
│                                      │
│ ¥201,832                             │  当前估值 22sp Bold
│ 今日利息 +¥15.07                     │  涨跌色 16sp
│                                      │
│ ───────────────────────────────────  │
│                                      │
│ 本金 ¥200,000 · 利率 2.75%          │  12sp
│ 已持 121 天 · 到期 2027/03/15       │  12sp
└──────────────────────────────────────┘
```

### 7.10 现金页面

```
┌──────────────────────────────────────┐
│                                      │
│  ┌────────────────────────────────┐  │
│  │  现金余额                      │  │  Elevated Card
│  │  ¥ 45,000.00                   │  │  36sp Bodoni Moda 700
│  │                                │  │
│  │  [+ 入金]  [- 出金]            │  │  两个按钮
│  └────────────────────────────────┘  │
│                                      │
│  资金流水                             │  16sp Jost 600
│                                      │
│  ┌────────────────────────────────┐  │
│  │ 2026-05-20                     │  │
│  │ 手动存入              +10,000  │  │
│  │ ────────────────────────────  │  │
│  │ 2026-05-15                     │  │
│  │ 手动取出               -2,000  │  │
│  │ ────────────────────────────  │  │
│  │ 2026-03-01                     │  │
│  │ 工行定期到期入账       +50,401  │  │  自动生成的流水
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘
```

- 入金/出金弹窗：输入金额 + 备注 → 确认 → 生成流水、更新余额
- 存款到期入账自动生成流水，无需手动操作
- 流水列表按日期降序

### 7.11 信用页面（应收 + 负债）

```
┌──────────────────────────────────────┐
│                                      │
│  ┌─ 应收款 ──────────────── [+] ─┐  │
│  │                                │  │
│  │  张三                 ¥20,000  │  │  Elevated Card
│  │  2026-02-10 借出              │  │
│  │  预计 2026-08 归还            │  │
│  │  ─────────────────────────── │  │
│  │  王五                 ¥10,000  │  │
│  │                                │  │
│  │  合计应收  ¥30,000             │  │
│  └────────────────────────────────┘  │
│                                      │
│  ┌─ 负债 ────────────────── [+] ─┐  │
│  │                                │  │
│  │  招行信用卡           ¥35,000  │  │  Elevated Card
│  │  李四                 ¥25,000  │  │
│  │                                │  │
│  │  合计负债  ¥60,000             │  │
│  └────────────────────────────────┘  │
│                                      │
│  应收净额  -¥30,000                   │  底部汇总
└──────────────────────────────────────┘
```

- 两个区域独立，各自有 [+] 新增按钮
- 新增应收：姓名、金额、日期、预计归还日（可选）、备注
- 新增负债：名称、金额、日期、利率（可选）、备注
- 底部固定显示应收净额（应收 - 负债）

### 7.12 设置页（Page 9）— 备份与恢复

```
┌──────────────────────────────────────┐
│  设置                                 │
│                                      │
│  ┌─ 账号管理 ─────────────────────┐  │
│  │  当前账号：张三                │  │
│  │  切换账号 / 修改PIN / 新账号   │  │
│  └────────────────────────────────┘  │
│                                      │
│  ┌─ 数据备份 ─────────────────────┐  │
│  │  ┌──────────────────────────┐  │  │
│  │  │  一键备份                │  │  │  ← 点击 → SAF 选路径 → 导出 JSON
│  │  │  导出完整数据快照        │  │  │
│  │  └──────────────────────────┘  │  │
│  │  ┌──────────────────────────┐  │  │
│  │  │  一键恢复                │  │  │  ← 点击 → 确认 → SAF 选文件 → 预览 → 恢复
│  │  │  从备份文件恢复所有数据   │  │  │
│  │  └──────────────────────────┘  │  │
│  └────────────────────────────────┘  │
│                                      │
│  ┌─ 数据管理 ─────────────────────┐  │
│  │  数据导出 (CSV)                │  │
│  │  数据导入 (CSV)                │  │
│  │  生成测试数据                  │  │
│  │  一键重算收益                  │  │
│  │  清空数据 (红色)               │  │
│  └────────────────────────────────┘  │
│                                      │
│  ┌─ 汇率基准 ─────────────────────┐  │
│  │  当前汇率 + 日期               │  │
│  │  手动刷新                      │  │
│  └────────────────────────────────┘  │
│                                      │
│  ┌─ 显示倍率 ─────────────────────┐  │
│  │  ○ 10%  ○ 50%  ● 100%        │  │
│  │  所有金额显示将乘以所选倍率    │  │
│  └────────────────────────────────┘  │
└──────────────────────────────────────┘
```

**备份卡片规格**：
- 两个卡片水平排列（`Row` + `weight(1f)`）
- 左侧「一键备份」：`OutlinedCard`，图标 `SaveAlt`，标题 + 副标题"导出完整数据快照"
- 右侧「一键恢复」：`OutlinedCard`，图标 `Restore`，标题 + 副标题"从备份文件恢复所有数据"

**恢复确认弹窗**：
```
┌──────────────────────────────────┐
│  ⚠ 确认恢复数据                  │
│                                  │
│  将用备份文件覆盖当前账号所有数据 │
│  此操作不可撤销                  │
│                                  │
│  备份日期：2026-05-22 15:30      │
│  存款 3 笔 · 持仓 6 只           │
│  价格快照 540 条 · 汇率 90 条    │
│                                  │
│  [取消]            [确认恢复]     │
└──────────────────────────────────┘
```

**一键重算收益**：
- 位置：数据管理卡片内，清空数据上方
- 样式：与「生成测试数据」一致，`ElevatedCard` + 标题 + 副标题
- 副标题：「删除所有历史收益汇总并重新计算」
- 点击弹出确认弹窗，确认后调用 `BackfillEngine.markDirtyAndBackfill()` 从最早资产日期开始全量重算
- 完成后 Toast 提示

**重算确认弹窗**：
```
┌──────────────────────────────────┐
│  ⚠ 确认重算收益                  │
│                                  │
│  将删除所有每日收益汇总数据      │
│  并从最早资产日期重新计算        │
│                                  │
│  此操作不会影响存款、持仓等      │
│  原始数据，但无法撤销            │
│                                  │
│  [取消]            [确认重算]     │
└──────────────────────────────────┘
```

**显示倍率卡片规格**：
- 位置：汇率基准卡片下方，关于卡片上方
- 样式：`ElevatedCard`，标题「显示倍率」
- 选项：三选一 `SingleChoiceSegmentedButtonRow` 或 `Row` + `FilterChip`
  - 10%（所有金额 × 0.1）
  - 50%（所有金额 × 0.5）
  - 100%（真实金额，默认）
- 副标题说明：「所有页面的金额显示将乘以所选倍率」
- 选择后立即生效，持久化存储

**显示倍率数据流**：
- 存储：SharedPreferences（`display_multiplier` key，默认 1.0）
- 提供方：`DisplaySettings` 单例，通过 `StateFlow<BigDecimal>` 暴露倍率
- 消费方：各 ViewModel 收集倍率 Flow，传给 `formatMoney` 函数
- 所有 `formatMoney` 统一乘以倍率后再格式化

---

## 八、收益日历重设计

### 8.1 日视图（月历）

```
┌──────────────────────────────────────┐
│    日       周       月       年      │  文字切换器
│                                      │
│    ◀      2026年 5月      ▶          │  月份导航
│                                      │
│  一   二   三   四   五   六   日      │
│                 1    2    3    4     │  数字 + 收益金额
│  5    6    7    8    9    10   11    │  涨红底/跌绿底
│ 12   13   14   15   16   17   18    │  深浅表示幅度
│ 19   20  [21]  22   23   24   25    │  选中 = 金色边框
│ 26   27   28   29   30   31         │
│                                      │
│ ┌──────────────────────────────────┐ │
│ │ 5月21日               +320.50    │ │  点击弹出的明细卡
│ │ 🏦 存款 +15   📊 股票 +200       │ │
│ │ 💰 基金 +150  🥇 黄金 +15        │ │
│ └──────────────────────────────────┘ │
└──────────────────────────────────────┘
```

### 8.2 年视图（热力图）

GitHub contributions 风格，颜色映射：

```
深绿  ←  大涨日 (> +2%)
浅绿  ←  小涨日 (> 0%)
白色  ←  无变化
浅红  ←  小跌日 (< 0%)
深红  ←  大跌日 (< -2%)
灰色  ←  无数据
```

---

## 九、动画规范

| 场景 | 动画 | 时长 |
|------|------|------|
| 数字变化 | `animateIntAsState` / `animateFloatAsState` | 500ms, easeOut |
| 卡片进入 | `AnimatedVisibility` + verticalSlide + fade | 400ms, easeOut |
| 页面切换 | fade(200ms) + horizontalSlide(300ms) | 300ms |
| 卡片点击 | `animateFloatAsState` scale 1.0 → 0.97 → 1.0 | 150ms, spring |
| Tab/时间切换指示线 | `animateFloatAsState` 位置过渡 | 250ms, easeInOut |
| 下拉刷新 | Material 3 内置 PullToRefresh | 默认 |
| 数字滚动 | 使用 `animateFloatAsState` 模拟数字递增 | 600ms, easeOut |
| 涟漪 | Material 3 默认 `ripple()` | 默认 |

> 所有动画需检查 `prefers-reduced-motion`，若开启则 duration = 0。

---

## 十、图标规范

- 不再使用 emoji（🏦📊💰🥇 → 全部替换）
- 使用 Material Icons Extended：

| 原来 | 替换为 |
|------|--------|
| 💰 存款 | `AccountBalance` |
| 📈 股票 | `TrendingUp` |
| 💵 基金 | `WaterDrop`（水滴 = 流动 = 资金） |
| 🥇 黄金 | `Diamond` |

- 图标大小：24dp 统一 `Modifier.size(24.dp)`
- 导航栏：Outlined（未选中）/ Filled（选中）
- 所有图标使用 `MaterialTheme.colorScheme.onSurface`

---

## 十一、实现要点

### 11.1 Theme.kt

- 新增自定义 `ColorScheme`，不依赖 `dynamicColor`（Material You 取色不可控，无法保证金色点缀的一致性）
- 浅色/深色两套 `ColorScheme`，手动定义
- 新增 `FinancialTypography` 使用 Bodoni Moda + Jost

### 11.2 字体下载

```kotlin
val BodoniModaFont = GoogleFont("Bodoni Moda", FontWeight.Bold)
val JostFont = GoogleFont("Jost", FontWeight.Normal)
// 使用 downloadableFonts
val fontProvider = GoogleFontsProvider()
val bodoniRequest = FontRequest(
    fontProvider.authority,
    fontProvider.packageName,
    "Bodoni Moda",
    listOf(listOf("wght@700")),
    listOf()
)
```

### 11.3 现有 PRD 已满足的部分

以下 PRD 第 15 章的设计意图与本次重设计一致，保留：

- 15.1：总资产放最上面 + 今日收益为子信息
- 15.2：三列卡片等高
- 15.3：纯文字时间切换器（本次改为金色指示线）
- 15.4：去掉「全部」Tab
- 15.5：白色卡片 + 阴影替代灰色 surfaceVariant

以下需要**进一步升级**：

- emoji → Material Icons
- Material You dynamicColor → 固定 Premium 配色
- 新增 Bodoni Moda + Jost 字体
- 新增资产占比迷你条
- 新增卡片微交互动画
- 金色点缀体系贯穿全局

---

## 十二、渐进式实施路径（v3）

### 阶段总览

| 阶段 | 范围 | 影响文件 | 依赖 |
|------|------|----------|------|
| A — 全局 Pager 骨架 | AppNavigation, MainScreen, BottomNavBar | 导航层 | 无 |
| B — Section 指示器组件 | 新建 `SectionIndicator.kt` | 新组件 | A |
| C — 首页适配 | HomeScreen, HomeViewModel | 首页 | A |
| D — 投资段（股票/基金/黄金） | HoldingsScreen → 拆入 Pager pages 1-3 | 持仓 | A, B |
| E — 存款段（持有中/已到期） | DepositCard → 拆入 Pager pages 4-5 | 存款 | A, B |
| F — 现金 + 信用 | CashScreen, CreditScreen → Pager pages 6-7 | 现金/信用 | A |
| G — 收益 + 设置 | EarningsScreen, SettingsScreen → Pager pages 8-9 | 收益/设置 | A |
| H — 收尾动画 + 验收 | 全局动画、边界处理、prefers-reduced-motion | 全局 | 全部 |

### 各阶段详细

#### A — 全局 Pager 骨架

- 创建 `MainPagerScreen.kt`，包含 10 页的 `HorizontalPager`
- `userScrollEnabled = true`，支持全程手动滑动
- `BottomNavBar` 改为接收 `currentPage: Int`，点击 = `animateScrollToPage`
- 底部导航高亮逻辑：page 0 → 首页，1-7 → 持仓，8 → 收益，9 → 设置
- 移除旧的 `NavHost` 页面跳转（首页/持仓/收益/设置之间的导航改为 Pager 滑动）
- 详情页（`HoldingDetailScreen`、`AddHoldingScreen` 等）保留 `NavHost` 栈

#### B — Section 指示器组件

- 新建 `SectionIndicator.kt`
- 参数：`categoryName`, `subItems: List<String>`, `currentIndex: Int`
- 渲染：分类标签 + 子项名称列表（当前加粗金色）+ 圆点
- 纯展示，不参与交互
- 仅在 page 1-5 显示（投资段 + 存款段）
- 其他 page 显示简单标题

#### C — 首页适配

- `HomeScreen` 嵌入 Pager page 0
- 资产网格点击：`scrollToPage(n)` 替代 `navigate`
- 其他内容（净资产卡片、今日收益、走势图）保持不变

#### D — 投资段

- 将 `HoldingsScreen` 的股票/基金/黄金三页拆入 Pager pages 1-3
- 每页独立 `LazyColumn` + FAB
- 共享 `HoldingsViewModel`
- 顶部 Section 指示器自动跟随 page 位置

#### E — 存款段

- 将持有中/已到期拆入 Pager pages 4-5
- 共享数据源

#### F — 现金 + 信用

- 原来 `CashScreen`、`CreditScreen` 不再独立 navigate
- 直接嵌入 Pager pages 6-7
- 添加/编辑操作仍走 NavHost

#### G — 收益 + 设置

- 嵌入 Pager pages 8-9

#### H — 收尾

- 全局动画：页面切换 `animateScrollToPage` 流畅
- 边界处理：page 0 不能左滑，page 9 不能右滑
- `prefers-reduced-motion` 检查

---

## 十三、v16 表单重设计（2026-05-22）

### 13.1 新增存款表单（去掉名称）

```
┌──────────────────────────────────────┐
│  新增存款                             │
│                                      │
│  银行/平台 *                          │
│  ┌──────────────────────────────┐    │
│  │ 工商银行                      │    │
│  └──────────────────────────────┘    │
│                                      │
│  币种 *                  [CNY ▼]    │
│                                      │
│  本金 *                               │
│  ┌──────────────────────────────┐    │
│  │ 200,000                       │    │
│  └──────────────────────────────┘    │
│                                      │
│  年化利率 *                           │
│  ┌──────────────────────────────┐    │
│  │ 2.75                          │    │
│  └──────────────────────────────┘    │
│                                      │
│  存入日期 *          [2025-01-15]    │
│  到期日期            [2027-03-15]    │
│                                      │
│  备注                                 │
│  ┌──────────────────────────────┐    │
│  │                               │    │
│  └──────────────────────────────┘    │
│                                      │
│  从现金中扣除          [ Switch ]     │
│                                      │
│  [        保存        ]              │
└──────────────────────────────────────┘
```

> 变更：去掉"存款名称"字段，银行/平台即作为唯一标识。保存时 `name = bank`。

### 13.2 新增股票表单（输入时自动搜索）

```
┌──────────────────────────────────────┐
│  新增股票                             │
│                                      │
│  代码 *                               │
│  ┌──────────────────────────────┐    │
│  │ 6005                     ⟳   │    │  ← 输入时右侧转圈=搜索中
│  └──────────────────────────────┘    │
│  ┌─ 搜索结果（下拉覆盖）─────────┐    │
│  │ 600519  贵州茅台    A股  >  │    │  ← 点击自动填入
│  │ 600050  中国联通    A股  >  │    │
│  │ 600030  中信证券    A股  >  │    │
│  │ ...                         │    │
│  └──────────────────────────────┘    │
│                                      │
│  名称                                 │
│  ┌──────────────────────────────┐    │
│  │ 贵州茅台          (自动填入)   │    │
│  └──────────────────────────────┘    │
│                                      │
│  市场                                 │
│  ┌──────────────────────────────┐    │
│  │ CN                  (自动填入)│    │
│  └──────────────────────────────┘    │
│                                      │
│  持有数量 (股) *                      │
│  ┌──────────────────────────────┐    │
│  │ 100                           │    │
│  └──────────────────────────────┘    │
│                                      │
│  成本价 *                             │
│  ┌──────────────────────────────┐    │
│  │ 1,680                         │    │
│  └──────────────────────────────┘    │
│                                      │
│  买入日期 *          [2025-03-01]    │
│                                      │
│  备注                                 │
│  从现金中扣除          [ Switch ]     │
│                                      │
│  [        保存        ]              │
└──────────────────────────────────────┘
```

**搜索交互规格**：
- 用户输入 ≥1 个字符 → 300ms 去抖 → 自动调用 `PriceService.searchAll(query)`
- 搜索范围：A 股（东方财富）+ 美股（Finnhub）+ 港股（东方财富港股 API）
- 搜索中：输入框右侧显示小转圈（`CircularProgressIndicator`, 16dp）
- 有结果：下拉列表出现在输入框下方，最多 8 条，超出可滚动
- 选中某条：自动填入代码、名称、市场 → 下拉消失
- 无结果：显示"未找到匹配结果"
- 点击输入框外区域 / 清空输入 → 下拉消失
- 用户可忽略搜索结果，手动填写所有字段

### 13.3 新增基金表单（自动搜索）

与股票表单交互一致，区别：
- 搜索调用同一 `searchAll()`，结果类型过滤 `type=FUND`
- 数量标签"持有份额"，价格标签"成本净值"
- 无市场字段（国内基金默认 CN）

### 13.4 新增黄金表单（极简）

```
┌──────────────────────────────────────┐
│  新增黄金                             │
│                                      │
│  克数 *                               │
│  ┌──────────────────────────────┐    │
│  │ 50                            │    │
│  └──────────────────────────────┘    │
│                                      │
│  单价 (元/克) *                       │
│  ┌──────────────────────────────┐    │
│  │ 450                           │    │
│  └──────────────────────────────┘    │
│                                      │
│  ┌────────────────────────────────┐  │
│  │  当前参考金价  ¥458.50/克       │  │  ← 实时金价参考
│  │  上海金交所 AU9999 · 更新于15:30 │  │
│  └────────────────────────────────┘  │
│                                      │
│  买入日期 *          [2025-01-01]    │
│                                      │
│  ┌────────────────────────────────┐  │
│  │                                │  │
│  │  买入总价  ¥22,500.00          │  │  ← 自动计算 = 克数 × 单价
│  │                                │  │
│  └────────────────────────────────┘  │
│                                      │
│  备注                                 │
│  从现金中扣除          [ Switch ]     │
│                                      │
│  [        保存        ]              │
└──────────────────────────────────────┘
```

> 变更：完全去掉代码、名称、形式字段。只需克数 + 单价 + 日期。买入总价自动计算（克数 × 单价）并实时展示。

### 13.5 持仓详情页 — 加仓/减仓按钮

```
┌─────────────────────────────────┐
│ ← 贵州茅台 (600519)              │
│                                  │
│ ¥1,720.00                        │
│ +20.00 (+1.18%) 今日             │
│                                  │
│ ┌──────────────────────────────┐ │
│ │ 价格走势                       │ │
│ └──────────────────────────────┘ │
│                                  │
│ ┌────────────┐ ┌────────────┐  │
│ │ 持仓盈亏     │ │ 今日盈亏     │  │
│ │ +¥4,000   │ │ +¥200     │  │
│ └────────────┘ └────────────┘  │
│                                  │
│ 持有 100 股 · 成本均价 ¥1,680     │
│ 总成本 ¥168,000 · 市值 ¥172,000  │
│                                  │
│ [ + 加仓 ]        [ - 减仓 ]     │  ← 新增操作按钮
│                                  │
│ ▶ 交易记录                       │
│   2025-06-01  买入 50股 @1750   │
│   2025-05-20  买入 100股 @1680  │
│                                  │
│ [编辑]            [删除]         │
└─────────────────────────────────┘
```

### 13.6 加仓弹窗

```
┌─────────────────────────────────┐
│  加仓 — 贵州茅台                  │
│                                  │
│  当前：100 股 · 成本均价 ¥1,680   │
│  ─────────────────────────────── │
│                                  │
│  买入数量 (股)                    │
│  ┌────────────────────────────┐  │
│  │ 50                         │  │
│  └────────────────────────────┘  │
│                                  │
│  买入价格 (元)                    │
│  ┌────────────────────────────┐  │
│  │ 1,750                       │  │
│  └────────────────────────────┘  │
│                                  │
│  买入日期        [2025-06-01]    │
│                                  │
│  ─────────────────────────────── │
│  加仓后预览：                     │
│  持有 150 股                     │
│  成本均价 ¥1,703.33              │
│  总成本 ¥255,500                │
│                                  │
│  从现金中扣除      [ Switch ]     │
│                                  │
│  [取消]            [确认加仓]     │
└─────────────────────────────────┘
```

### 13.7 减仓弹窗

```
┌─────────────────────────────────┐
│  减仓 — 贵州茅台                  │
│                                  │
│  当前：100 股 · 成本均价 ¥1,680   │
│  ─────────────────────────────── │
│                                  │
│  卖出数量 (股)                    │
│  ┌────────────────────────────┐  │
│  │ 40                         │  │
│  └────────────────────────────┘  │
│                                  │
│  卖出价格 (元)                    │
│  ┌────────────────────────────┐  │
│  │ 1,850                       │  │
│  └────────────────────────────┘  │
│                                  │
│  卖出日期        [2025-06-05]    │
│                                  │
│  ─────────────────────────────── │
│  减仓后预览：                     │
│  剩余 60 股                      │
│  实现盈亏 +¥6,800 (+38.2%)        │
│  剩余成本均价 ¥1,680             │
│                                  │
│  收入计入现金      [ Switch ]     │
│                                  │
│  [取消]            [确认减仓]     │
└─────────────────────────────────┘
```

### 13.8 状态说明

| 状态 | 含义 | 显示 |
|------|------|------|
| `active` | 正常持有 | 显示加仓/减仓按钮 |
| `closed` | 已全部卖出 | 灰色卡片，仅保留历史记录，不显示操作按钮 |

### 13.9 交互细节

| 操作 | 行为 |
|------|------|
| 减仓数量 = 持仓数量 | 全部清仓 → 二次确认"将卖出全部持仓，确认？" → Holding.status → "closed" |
| 减仓数量 > 持仓数量 | Toast "卖出数量不能超过当前持仓" |
| 加仓数量 ≤ 0 | Toast "买入数量必须大于 0" |
| 加仓确认 | 更新 Holding.quantity += n, costPrice = 加权均价, 插入 Transaction(BUY) |
| 减仓确认 | 更新 Holding.quantity -= n, 插入 Transaction(SELL)，若 quantity=0 则 status="closed" |

---

## 十四、持仓分组重设计（v17 — 2026-05-23 设计稿）

### 14.0 设计目标

**问题**：当前持仓列表是扁平列表，同一平台的存款、同一只股票的多次买入分散展示，信息碎片化。

**方案**：
- **存款**：同一银行/平台（`bank` 字段相同）的存单合并为一个银行组卡片，点击进入银行详情看到具体存单
- **股票/基金**：同一代码（`symbol` 相同）的持仓合并为一个标的卡片，卡片内展示买入记录
- **黄金**：保持扁平（通常每个用户只有 1-2 笔黄金）

### 14.1 存款 — 银行组卡片（Page 4：持有中）

```
┌──────────────────────────────────────────────┐
│  ┌──────────────────────────────────────┐    │
│  │  存款                                │    │
│  │  持有中  ·  已到期                   │    │
│  │  ●   ○                              │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  ┌─ 工商银行 ──────────────────────────┐    │
│  │                                     │    │
│  │  ┌──────────────────────────────┐   │    │
│  │  │ 🔵 工商银行           3 笔存单 │   │    │  ← 蓝色图标 + 银行名 + 存单数
│  │  │                              │   │    │
│  │  │  本金合计     ¥500,000       │   │    │
│  │  │  当前估值     ¥512,350       │   │    │  ← 本金+累计利息
│  │  │  今日利息     +¥45.20        │   │    │  ← 涨跌色
│  │  │                              │   │    │
│  │  │  ██████████████░░░  72%     │   │    │  ← 加权平均进度条
│  │  │                              │   │    │
│  │  │  最早到期  2027/03/15        │   │    │  ← 最近到期日
│  │  └──────────────────────────────┘   │    │
│  │                              [>]    │    │  ← 点击进入银行详情
│  └─────────────────────────────────────┘    │
│                                              │
│  ┌─ 招商银行 ──────────────────────────┐    │
│  │  🔵 招商银行           2 笔存单      │    │
│  │  本金合计 ¥200,000                  │    │
│  │  ...                               │    │
│  └─────────────────────────────────────┘    │
│                                              │
│  ┌─ Bank of America ───────────────────┐    │
│  │  🔵 BoA                1 笔存单     │    │
│  │  本金合计 $8,000                    │    │
│  │  ...                               │    │
│  └─────────────────────────────────────┘    │
│                                              │
│  [+] 添加存款 FAB                           │
└──────────────────────────────────────────────┘
```

### 银行组卡片规格

| 属性 | 值 |
|------|-----|
| 卡片类型 | `ElevatedCard`，16dp 圆角，elevation=2dp |
| 左侧色条 | 4dp 宽，`#4A6FA5`（存款蓝），全高 |
| 银行名 | 16sp Jost 600，`onSurface` |
| 存单数标签 | 12sp Jost 400，`onSurfaceVariant`，右对齐 |
| 本金合计 | 14sp Jost 500，`onSurface`，格式 `¥xxx` |
| 当前估值 | 16sp Jost 600，`onSurface`（估值 = 本金 + 累计利息） |
| 今日利息 | 14sp Jost 500，涨跌色 |
| 进度条 | `LinearProgressIndicator`，4dp 高，颜色 `#4A6FA5`，圆角 2dp |
| 进度值 | 各存单进度的**本金加权平均** |
| 最早到期 | 12sp Jost 400，`onSurfaceVariant`，取该银行下最近的到期日 |
| 右侧箭头 | 24dp `ChevronRight` 图标，`onSurfaceVariant` |
| 卡片内边距 | 16dp |

### 银行组交互

| 操作 | 行为 |
|------|------|
| 点击银行卡片 | 导航至 `BankDepositsScreen(bankName, status)` |
| 长按银行卡片 | 弹出菜单：添加存单到该银行 / 查看所有存单 |
| 左滑银行卡片 | 不可删除（银行是分组维度，不是实体） |

---

### 14.2 存款 — 银行详情页（BankDepositsScreen）

点击银行卡片后进入，展示该银行下所有存单。

```
┌──────────────────────────────────────────────┐
│ ← 工商银行                       [+] 添加存单 │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │  工商银行 · 3 笔存单                  │    │  ← 汇总卡片
│  │  ─────────────────────────────────── │    │
│  │                                      │    │
│  │  本金合计         ¥500,000           │    │
│  │  累计利息         ¥12,350            │    │
│  │  当前估值         ¥512,350           │    │
│  │  今日利息         +¥45.20            │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  ─── 存单列表 ─────────────────────────────  │
│                                              │
│  ┌─ 定期存单 #1 ────────────────────────┐    │
│  │                                     │    │
│  │  本金 ¥200,000 · 利率 2.75%         │    │  ← 标题行
│  │                                     │    │
│  │  当前估值  ¥201,832                 │    │  ← 16sp Bold
│  │  今日利息  +¥15.07                  │    │  ← 涨跌色
│  │                                     │    │
│  │  ───────────────────────────────    │    │
│  │                                     │    │
│  │  存入 2025/01/15 · 到期 2027/03/15 │    │  ← 日期范围
│  │  已持 121 天 / 共 790 天            │    │
│  │  ██████████████░░░░░  65%          │    │  ← 进度条
│  │                                     │    │
│  │  累计利息  ¥1,832                   │    │
│  │                                     │    │
│  │  [编辑]                    [删除]   │    │
│  └─────────────────────────────────────┘    │
│                                              │
│  ┌─ 定期存单 #2 ────────────────────────┐    │
│  │  本金 ¥200,000 · 利率 3.00%          │    │
│  │  ...                                 │    │
│  └─────────────────────────────────────┘    │
│                                              │
│  ┌─ 定期存单 #3 ────────────────────────┐    │
│  │  本金 ¥100,000 · 利率 2.50%          │    │
│  │  ...                                 │    │
│  └─────────────────────────────────────┘    │
└──────────────────────────────────────────────┘
```

### 银行详情页规格

| 元素 | 规格 |
|------|------|
| 顶部栏 | 返回箭头 + 银行名 + 右侧 [+] 快速添加存单 |
| 汇总卡片 | `ElevatedCard`，16dp 圆角，内边距 16dp |
| 存单卡片 | `ElevatedCard`，12dp 圆角，elevation=2dp，左侧 4dp 蓝色条 |
| 存单标题 | 14sp Jost 600 |
| 估值数字 | 16sp Jost 600 |
| 存单间距 | 12dp |
| 编辑/删除 | 底部 Row，文字按钮，删除红色 |

---

### 14.3 已到期存款 — 银行组卡片（Page 5）

与持有中结构一致，但：
- 银行组卡片半透明灰色调
- 进度条不显示
- 显示「已到期 ✓」标签替代进度
- 存单数 = 该银行下 status=matured/settled 的存款数
- 点击同样进入 BankDepositsScreen（status= matured）

```
┌─ 工商银行（已到期）──────────────────────┐
│  🔵 工商银行           2 笔存单  已到期 ✓ │
│  本息合计 ¥150,401                      │
│  2026/03/01 自动赎回到现金               │
└─────────────────────────────────────────┘
```

---

### 14.4 股票 — 标的组卡片（Page 1：投资·股票）

同一 `symbol` 的持仓合并为一张卡片，卡片内展示买入记录。

**卡片有两种状态：折叠（默认）/ 展开（显示买入记录）。**

#### 折叠态

```
┌──────────────────────────────────────────────┐
│  ┌──────────────────────────────────────┐    │
│  │  投资                                │    │
│  │  股票  ·  基金  ·  黄金              │    │
│  │  ●   ○   ○                          │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  ┌─ 贵州茅台 ──────────────────────────┐    │
│  │                                     │    │
│  │  ┌──────────────────────────────┐   │    │
│  │  │ 📈 贵州茅台           A股     │   │    │  ← 红色图标 + 名称 + 市场标签
│  │  │ 600519                        │   │    │  ← 代码，12sp 灰色
│  │  │                               │   │    │
│  │  │  ¥1,720.00  当前价            │   │    │  ← 20sp Bold
│  │  │  ▲ +20.00 (+1.18%) 今日      │   │    │  ← 涨跌色 14sp
│  │  │                               │   │    │
│  │  │  ┌─────────────────────────┐  │   │    │
│  │  │  │  [迷你走势图 · 30天]     │  │   │    │  ← 可选：小面积图
│  │  │  └─────────────────────────┘  │   │    │
│  │  │                               │   │    │
│  │  │  持有 150 股                   │   │    │
│  │  │  成本均价  ¥1,703.33           │   │    │  ← 加权平均
│  │  │  持仓盈亏  +¥2,500 (+0.98%)   │   │    │  ← 涨跌色
│  │  │  市值 ¥258,000                 │   │    │
│  │  │                               │   │    │
│  │  │  ▶ 2 笔买入记录        [展开] │   │    │  ← 点击展开
│  │  └──────────────────────────────┘   │    │
│  │                              [>]    │    │  ← 进入详情页
│  └─────────────────────────────────────┘    │
│                                              │
│  ┌─ 腾讯控股 ──────────────────────────┐    │
│  │  ...（同上结构）                     │    │
│  └─────────────────────────────────────┘    │
│                                              │
│  [+] 添加股票 FAB                           │
└──────────────────────────────────────────────┘
```

#### 展开态（点击「展开」后）

```
│  │  │                               │   │    │
│  │  │  ▼ 2 笔买入记录        [收起] │   │    │  ← 展开状态
│  │  │  ─────────────────────────── │   │    │
│  │  │                               │   │    │
│  │  │  ┌─ 买入 #2 ────────────┐    │   │    │
│  │  │  │ 2025/06/01  买入      │    │   │    │  ← 日期 + 类型标签
│  │  │  │ 50 股 @ ¥1,750.00     │    │   │    │  ← 数量 + 单价
│  │  │  │ ────────────────────  │    │   │    │
│  │  │  │ 成本 ¥87,500          │    │   │    │
│  │  │  │ 当前市值 ¥86,000      │    │   │    │
│  │  │  │ 盈亏 -¥1,500 (-1.71%) │    │   │    │  ← 跌绿色
│  │  │  └───────────────────────┘    │   │    │
│  │  │                               │   │    │
│  │  │  ┌─ 买入 #1（首次）─────┐    │   │    │
│  │  │  │ 2025/05/20  买入      │    │   │    │
│  │  │  │ 100 股 @ ¥1,680.00   │    │   │    │
│  │  │  │ ────────────────────  │    │   │    │
│  │  │  │ 成本 ¥168,000         │    │   │    │
│  │  │  │ 当前市值 ¥172,000     │    │   │    │
│  │  │  │ 盈亏 +¥4,000 (+2.38%) │    │   │    │  ← 涨红色
│  │  │  └───────────────────────┘    │   │    │
│  │  │                               │   │    │
│  │  │  [+ 加仓]          [- 减仓]   │   │    │  ← 操作按钮
│  │  └──────────────────────────────┘   │    │
```

> **展开/折叠**：点击「展开」→ 买入记录从底部滑出（`AnimatedVisibility` + `expandVertically`），箭头从 ▶ 变 ▼。折叠时不显示买入记录，节省列表空间。

### 股票组卡片规格

| 属性 | 值 |
|------|-----|
| 卡片 | `ElevatedCard`，16dp 圆角，elevation=2dp |
| 左侧色条 | 4dp 宽，`#C0392B`（股票红），全高 |
| 名称 | 16sp Jost 600，`onSurface` |
| 市场标签 | 10sp Jost 400，白色字，深灰底胶囊（8dp 圆角），如 `A股` `美股` `港股` |
| 代码 | 12sp Jost 400，`onSurfaceVariant` |
| 当前价 | 20sp Jost 700，`onSurface` |
| 今日涨跌 | 14sp Jost 500，涨跌色 |
| 迷你走势图 | 48dp 高，Vico `LineChart`，仅显示近 30 天趋势线，无坐标轴 |
| 持有信息 | 13sp Jost 400 |
| 持仓盈亏 | 14sp Jost 600，涨跌色 |
| 买入记录行 | 13sp Jost 500，`onSurface`，「▶ N 笔买入记录」+ 右侧「展开」文字按钮 |
| 展开后每条记录 | 12dp 圆角小卡片，`surfaceVariant` 背景，10dp 内边距 |
| 买入记录内文字 | 12sp Jost 400 |
| 买入盈亏 | 12sp Jost 500，涨跌色 |
| 加仓/减仓按钮 | 展开态底部，`OutlinedButton`，14sp |

### 迷你走势图规格

| 属性 | 值 |
|------|-----|
| 高度 | 48dp |
| 宽度 | 填满卡片宽度 - 32dp |
| 数据 | 近 30 天 PriceSnapshot |
| 线条 | 1.5dp 宽，涨红色/跌绿色（以首日为基准） |
| 填充 | 线条下方 10% 透明度渐变填充 |
| 坐标轴 | 无（极简，仅看趋势方向） |
| 加载中 | shimmer 占位 |
| 无数据 | 不显示该区域 |

### 股票组交互

| 操作 | 行为 |
|------|------|
| 点击「展开/收起」 | 展开/收起买入记录列表（300ms `animateContentSize`） |
| 点击卡片主体（非展开按钮） | 导航至 `HoldingDetailScreen` 详情页 |
| 点击「加仓」 | 弹出 `AddPositionDialog` |
| 点击「减仓」 | 弹出 `ReducePositionDialog` |
| 左滑卡片 | 删除该组（二次确认：将删除该标的所有买入记录） |

---

### 14.5 基金 — 标的组卡片（Page 2：投资·基金）

与股票组卡片结构完全相同，差异仅在视觉标识：

| 属性 | 股票 | 基金 |
|------|------|------|
| 左侧色条 | `#C0392B` | `#8B5CF6`（紫色） |
| 图标 | `TrendingUp` | `WaterDrop` |
| 市场标签 | A股/美股/港股 | 不显示（国内基金默认 CN） |
| 加仓/减仓 | ✅ | ✅ |
| FAB | 添加基金 | 添加基金 |

### 14.6 黄金 — 保持扁平（Page 3：投资·黄金）

黄金持仓数量少（通常 1-2 笔），**不做分组**。保持原有卡片样式。

若后续出现同一用户多笔黄金（如实物金条 + 纸黄金），可合并为一张黄金组卡片，规则同股票/基金。

---

### 14.7 交互细节对比

| 场景 | 旧方案 | 新方案 |
|------|--------|--------|
| 同一银行 3 笔存单 | 3 张独立卡片 | 1 张银行组卡片 → 点击进详情 |
| 同一股票 2 次买入 | 2 张独立卡片（可能同名混淆） | 1 张股票卡片，展开看 2 笔买入 |
| 同一基金 3 次申购 | 3 张独立卡片 | 1 张基金卡片，展开看 3 笔买入 |
| 查看单笔存单详情 | 列表直接看到 | 点进银行 → 列表中看到 → 编辑 |
| 查看单笔买入盈亏 | 需要在详情页看交易记录 | 展开即可看到每笔盈亏 |

---

### 14.8 数据层变更（供实现参考）

#### DepositDao 新增查询

```kotlin
// 按银行分组查询（持有中）
@Query("""
    SELECT * FROM deposits 
    WHERE accountId = :accountId AND status = 'active' 
    ORDER BY bank, startDate DESC
""")
fun getActiveGroupedByBank(accountId: Long): Flow<List<Deposit>>

// 某银行下所有存单
@Query("""
    SELECT * FROM deposits 
    WHERE accountId = :accountId AND bank = :bank AND status = :status
    ORDER BY startDate DESC
""")
fun getByBankAndStatus(bank: String, status: String, accountId: Long): Flow<List<Deposit>>

// 所有银行名（去重）
@Query("""
    SELECT DISTINCT bank FROM deposits 
    WHERE accountId = :accountId AND status = :status
    ORDER BY bank
""")
fun getDistinctBanks(status: String, accountId: Long): Flow<List<String>>
```

#### HoldingDao 新增查询

```kotlin
// 按 symbol 分组查询（active）
@Query("""
    SELECT * FROM holdings 
    WHERE accountId = :accountId AND type = :type AND status = 'active'
    ORDER BY symbol
""")
fun getByTypeGrouped(accountId: Long, type: String): Flow<List<Holding>>

// 某 symbol 下所有持仓
@Query("""
    SELECT * FROM holdings 
    WHERE accountId = :accountId AND symbol = :symbol AND status = 'active'
    ORDER BY costDate DESC
""")
fun getBySymbol(symbol: String, accountId: Long): Flow<List<Holding>>
```

#### 新增 Display 数据类

```kotlin
// 银行组展示数据
data class BankGroupDisplay(
    val bank: String,
    val depositCount: Int,
    val totalPrincipal: String,     // CNY 换算后
    val totalCurrentValue: String,  // 本金+累计利息
    val todayTotalInterest: String,
    val weightedProgress: Float,    // 本金加权平均
    val nearestMaturity: String,    // 最近到期日
    val currency: String
)

// 股票/基金组展示数据
data class HoldingGroupDisplay(
    val symbol: String,
    val name: String,
    val market: String,
    val type: String,
    val totalQuantity: String,
    val avgCost: String,           // 加权平均
    val currentPrice: String,
    val totalPnL: String,
    val totalPnLPct: String,
    val todayChange: String,
    val todayChangePct: String,
    val isUp: Boolean,
    val marketValue: String,
    val buyRecords: List<BuyRecordDisplay>,  // 买入记录
    val priceHistory: List<PriceSnapshot>    // 迷你走势图数据
)

data class BuyRecordDisplay(
    val transactionId: Long,
    val date: String,
    val type: String,           // BUY / SELL
    val quantity: String,
    val price: String,
    val cost: String,           // quantity × price
    val currentValue: String,   // quantity × currentPrice
    val pnl: String,
    val pnlPct: String,
    val isUp: Boolean
)
```

---

### 14.9 页面导航变更

```
Page 1 (投资·股票)
  │
  ├─ 点击股票卡片主体 → HoldingDetailScreen(holdingId = 合并后的主 holding id)
  │    └─ 展示完整详情 + 走势图 + 所有交易记录
  │
  ├─ 点击「展开」→ 卡片内展开买入记录（不导航）
  └─ 点击「加仓/减仓」→ PositionDialogs

Page 4 (存款·持有中)
  │
  ├─ 点击银行卡片 → BankDepositsScreen(bankName, status="active")
  │    ├─ 点击某存单 → 暂无存单详情页（暂用编辑页代替？）
  │    │    └─ 方案 A：直接进入 EditDepositScreen(depositId)
  │    │    └─ 方案 B：新做 DepositDetailScreen（存单详情，只读+编辑入口）
  │    ├─ 点击 [+] → AddDepositScreen（bank 预填为当前银行名）
  │    └─ 点击 [编辑] → EditDepositScreen(depositId)
  │
  └─ FAB → AddDepositScreen（bank 不预填）
```

> **建议**：暂用方案 A（编辑页），后续迭代再做存单详情页。

---

### 14.10 动画规范

| 场景 | 动画 | 时长 |
|------|------|------|
| 买入记录展开/折叠 | `AnimatedVisibility` + `expandVertically` + `fade` | 300ms easeOut |
| 进入银行详情 | NavHost 默认 fade + slide | 300ms |
| 卡片内展开箭头 | `animateFloatAsState` 旋转 0° → 180° | 200ms easeInOut |
| 迷你走势图加载 | shimmer → fadeIn | 500ms |

---

### 14.11 实施路径建议

| 阶段 | 范围 | 依赖 |
|------|------|------|
| I — 数据层 | 新增 DAO 查询 + Display 数据类 + ViewModel 改造 | 无 |
| II — 存款分组 | BankGroupCard + BankDepositsScreen + 导航 | I |
| III — 股票/基金分组 | HoldingGroupCard（折叠/展开）+ 迷你走势图 + 买入记录 | I |
| IV — 黄金保持 | 确认黄金页无需改动 | — |
| V — 已到期适配 | 已到期存款同样按银行分组 | II |
| VI — 收尾 | 边界情况、空状态、动画打磨、编译验证 | 全部 |

---

### 14.12 三级数据汇总体系

每一层入口都展示**总价值 + 当日盈亏**，用户在不同深度都能看到关键数字：

```
Level 0 — 首页 2×2 网格
  ├─ 投资卡片：¥420,000 · +820 今日
  ├─ 存款卡片：¥120,000 · +34 今日
  └─ ...

Level 1 — CategoryNavStrip 汇总行（page 1-7 顶部）
  ├─ 投资总计 ¥423,000 · 今日 +¥2,500
  ├─ 存款总计 ¥512,350 · 今日利息 +¥45.20
  ├─ 现金余额 ¥45,000
  └─ 应收净额 -¥30,000

Level 2 — SectionIndicator 子类汇总（page 1-5 顶部）
  ├─ 股票市值 ¥358,000 · 今日 +¥2,350 (+0.66%)
  ├─ 基金市值 ¥48,000 · 今日 +¥135 (+0.28%)
  ├─ 黄金市值 ¥17,000 · 今日 +¥15 (+0.09%)
  ├─ 持有中估值 ¥512,350 · 今日利息 +¥45.20
  └─ 已到期本息 ¥150,401

Level 3 — 分组卡片（列表主体）
  ├─ 工商银行：¥512,350 · 今日 +¥45.20
  ├─ 招商银行：¥xxx · 今日 +¥xx
  ├─ 贵州茅台：¥258,000 · 今日 +¥300
  ├─ 腾讯控股：¥xxx · 今日 +¥xx
  └─ ...
```

**数据流**：
```
HoldingsViewModel
  │
  ├─ toCategorySummary(type) → 各分类总计（Level 1）
  ├─ toSubCategorySummary(type) → 各子类总计（Level 2）
  ├─ toBankGroupList(status) → 银行组列表（Level 3 · 存款）
  └─ toHoldingGroupList(type) → 标的组列表（Level 3 · 股票/基金）
```

---

## 十五、v18 卡片重设计（2026-05-23）

### 15.1 问题

1. **股票分组卡片**：只有右上角小 `>` 图标可点击进入详情，整个卡片不可点；卡片信息层级杂乱，用户最关心的持仓市值和收益不突出
2. **存单卡片**：太高大，一页只能显示 2-3 张，信息分散在多行

### 15.2 股票组卡片（HoldingGroupCard）v18

**核心改动**：
- 整个卡片可点击（`ElevatedCard.onClick`）
- 移除右上角单独的 `ChevronRight` 点击图标
- 信息层级重组：持仓市值 + 今日涨跌 → 最突出，持仓盈亏 → 次突出，当前价/成本均价 → 辅助信息

```
┌──────────────────────────────────────┐
│ 🔴 600519  A股          贵州茅台      │  ← symbol, market tag, name
│                                      │
│ 持仓市值                              │  ← 11sp 标签
│ ¥258,000          今日 +¥300 (+0.66%)│  ← 22sp Bold 市值 + 涨跌色今日
│                                      │
│ ───────────────────────────────────  │
│                                      │
│ +¥2,500 (+0.98%)                     │  ← 16sp 涨跌色 持仓盈亏
│ 持有 150 股 · 均价 ¥1,703.33        │  ← 12sp 辅助信息
│ 现价 ¥1,720.00                       │
│                                      │
│ ▶ 2 笔买入记录                [展开]  │  ← 展开买入记录
└──────────────────────────────────────┘
```

| 属性 | 值 |
|------|-----|
| 卡片 | `ElevatedCard`，16dp 圆角，`onClick = onClick`（整卡可点） |
| 左侧色条 | 4dp 宽，类型色 |
| 内边距 | 14dp |
| 市值 | 22sp Bold，`onSurface` |
| 今日涨跌 | 15sp SemiBold，涨跌色 |
| 持仓盈亏 | 16sp SemiBold，涨跌色 |
| 辅助信息 | 12sp，`onSurfaceVariant` |
| 展开/折叠 | 行内文字按钮，仅展开时显示买入记录卡片 |

### 15.3 存单卡片（DepositCard）v18 compact

**核心改动**：
- 圆角从 16dp → 12dp
- 内边距从 16dp → 12dp 水平 / 10dp 垂直
- 进度条从 6dp → 4dp 高
- 信息合并为 4 行紧凑布局
- 去掉多余的分割线

```
┌──────────────────────────────────────┐
│ 🔵 工商银行                  ¥201,832│  ← 银行名 + 当前估值
│                                      │
│ 本金 ¥200,000 · 2.75%     今日 +15.07│  ← 本金/利率 + 今日利息
│                                      │
│ 2025/01/15 → 2027/03/15             │  ← 日期范围 + 天数/进度
│ 121/790天 (65%)                      │
│ ██████████████░░░░░                  │  ← 4dp 细进度条
│                                      │
│ 累计利息 ¥1,832          年利率 2.75%│  ← 累计利息 + 年利率
└──────────────────────────────────────┘
```

| 属性 | 旧值 | 新值 |
|------|------|------|
| 圆角 | 16dp | 12dp |
| 内边距 | 16dp | 12dp × 10dp |
| 进度条高度 | 6dp | 4dp |
| 分割线 | 2 条 | 0 条 |
| 字号 | titleMedium 为主 | titleSmall + 12sp 辅助 |
| 估值字号 | 20sp | 17sp |

### 15.4 银行详情存单卡片（BankDepositsScreen DepositCard）

```
┌──────────────────────────────────────┐
│ 🔵 定期存单 #1         ¥200,000 · 2.75%│  ← 名称 + 本金/利率
│                                      │
│ 当前估值  ¥201,832     今日利息 +15.07│  ← 估值 + 今日利息
│                                      │
│ 2025/01/15 → 2027/03/15             │  ← 日期
│ 121/790 天        累计利息 ¥1,832    │  ← 进度 + 累计利息
│ ██████████████░░░░░                  │  ← 4dp 细进度条
│                                      │
│              [编辑]        [删除]     │  ← 操作按钮右对齐
└──────────────────────────────────────┘
```

| 属性 | 旧值 | 新值 |
|------|------|------|
| 内边距 | 16dp | 12dp × 10dp |
| 进度条高度 | 5dp | 4dp |
| 分割线 | 3 条 | 0 条 |
| 编辑/删除图标 | 16dp | 14dp |
| 按钮字号 | 13sp | 12sp |
